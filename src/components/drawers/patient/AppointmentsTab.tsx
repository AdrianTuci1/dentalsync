// AppointmentsTab.tsx
import React from 'react';
import { Typography, Box } from '@mui/material';

const AppointmentsTab: React.FC = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="body1">Appointments content goes here.</Typography>
  </Box>
);

export default AppointmentsTab;
