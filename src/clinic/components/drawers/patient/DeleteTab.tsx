// DeleteTab.tsx
import React from 'react';
import { Typography, Box } from '@mui/material';

const DeleteTab: React.FC = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="body1">Delete content goes here.</Typography>
  </Box>
);

export default DeleteTab;
