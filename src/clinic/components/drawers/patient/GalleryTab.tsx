// GalleryTab.tsx
import React from 'react';
import { Typography, Box } from '@mui/material';

const GalleryTab: React.FC = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="body1">Gallery content goes here.</Typography>
  </Box>
);

export default GalleryTab;
